name: DeckForge Production Deploy

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Setup Node environment
        uses: actions/setup-node@v4
        with:
          node-version: 20
          # We removed "cache: npm" here because you don't have a package-lock.json

      - name: Install & Build
        run: |
          npm install
          npm run build

      - name: Azure CLI Upload
        uses: azure/CLI@v1
        with:
          inline_script: |
            az storage blob upload-batch \
              --connection-string "${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}" \
              --source dist \
              --destination '$web' \
              --overwrite true
